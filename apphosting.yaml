# This file is for configuring a Firebase App Hosting backend.
# If you are deploying to a different platform like Vercel, this file is not needed and can be safely removed.
# https://firebase.google.com/docs/app-hosting/configure

runConfig:
  # Increase this value if you'd like to automatically spin up
  # more instances in response to increased traffic.
  maxInstances: 1
